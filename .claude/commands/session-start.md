---
description: Refresh docs/agent/SESSION_START.md from current operating docs
---

You are refreshing the one-page session snapshot.

Steps:

1. Read these in order and synthesize the current situation:
   - `docs/operations/current-state.md`
   - `docs/operations/todo-plan.md`
   - `docs/operations/approval-queue.md`
   - `docs/project/product-brief.md`
2. Open `docs/agent/SESSION_START.md`.
3. Update the following sections only:
   - `Last refreshed` (use today's date in `YYYY-MM-DD (UTC±HH:MM)` form)
   - `Current phase`
   - `Active focus`
   - `What is safe to do without approval`
   - `What requires approval before execution`
   - `Critical unknowns`
4. Keep the file under ~150 lines. Strip stale bullets that no longer reflect reality.
5. Do NOT inline policy text — link to `AGENTS.md` instead.
6. Show the diff to the user before saving.

Do not touch any other doc in this command.
